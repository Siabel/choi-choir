import MainLogo from "@/components/Main/MainLogo";

export default function HomePage() {
  return (
    <section className="min-h-screen bg-primary-bg flex items-center justify-center">
      <MainLogo />
    </section>
  );
}